const add = require('./Section2Utils.js');
//const name = "Edward";
/* let [a, b] = [5, 10];
console.log(add(a, b)); */
//*Define and use a function in a new file
/* const sort = require('./challenge.js');
console.log(sort.sub(10, 2)); */
/* let arr = [1, -2, 10, 49, 10];
console.log(sort(arr).forEach((element, index) => {
  console.log(`${index}: ${element}`);
})) */;
const getNotes = require('./challenge.js')
console.log(getNotes());


